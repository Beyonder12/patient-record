//this script will make as below the model's properties for each command node .
/*
module.exports = function(app, cb) {
  app.models.Patient.create({
    firstName: 'honda',
    lastName: 'civic',
    Address: 'honda',
    age: 22
  }, function(err, car) {
    if (err)
      return cb(err);

    console.log('A `car` instance has been created from a boot script:', car);

    cb();
  });
};
*/
